# macOS
.DS_Store

# 编辑器
.idea/
*.iml
.vscode/

# 日志 / 临时
*.log

# 凭证 / 密钥(绝不进 git)
.env
.env.*
*.pem
refs/

# 签名 keystore / 口令(绝不进 git —— 仓库是 public)
*.jks
*.keystore
keystore.properties

# 本地工具 / 个人 skill(与 app 无关,纯本地需求)
.claude/

# 构建产物 / 大二进制(不进 git;按需本地 build)
# xraybridge.aar = SSH-over-443 隧道的 gomobile 产物(33MB),换机重跑 xray-bridge/build.sh 重建
android/app/libs/
*.aar
*.so
